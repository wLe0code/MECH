# Stub de faster_whisper: solo import, la transcripción real no se prueba aquí.
class WhisperModel:
    def __init__(self, *a, **k):
        pass

    def transcribe(self, *a, **k):
        return [], type("I", (), {"language": "es"})()